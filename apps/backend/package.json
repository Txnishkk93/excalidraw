{
  "name": "backend",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "tsc -b",
    "start": "node dist/index.js",
    "dev": "bun run build && bun run dev"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "7.0.2"
  },
  "dependencies": {
    "@repo/typescript-config": "workspace:*",
    "@types/express": "^5.0.6",
    "express": "^5.2.1"
  }
}